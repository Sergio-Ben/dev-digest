/** i18n namespace for the whole /conventions route. */
export const NS = "conventions";

/** Skeleton rows rendered while the first payload loads. */
export const SKELETON_ROWS = 3;

export const MINUTE_MS = 60_000;
export const HOUR_MS = 60 * MINUTE_MS;
export const DAY_MS = 24 * HOUR_MS;
