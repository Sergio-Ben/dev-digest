/** Constants for the evals module. */

/** Default page size for "recent runs" style listings (dashboard, history). */
export const DEFAULT_RUN_LIST_LIMIT = 50;

/** Default page size for "recent batches" style listings (dashboard, compare picker). */
export const DEFAULT_BATCH_LIST_LIMIT = 50;
